{
  "name": "hanquoc-classroom-api",
  "version": "2.3.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "build": "node --check src/index.js && node --check src/app.js && node --check src/ai.js && node --check src/auth.js && node --check src/config.js && node --check src/db.js && node --check src/monitoring.js && node --check src/rateLimit.js && node --check src/settings.js && node --check src/textbook.js && node --check scripts/init-db.js && node --check scripts/seed-db.js && node --check scripts/backup-db.js && node --check scripts/restore-db.js",
    "db:init": "node scripts/init-db.js",
    "db:seed": "node scripts/seed-db.js",
    "test": "vitest run",
    "db:backup": "node scripts/backup-db.js",
    "db:restore": "node scripts/restore-db.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.23.2",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "vitest": "^3.2.4"
  }
}
